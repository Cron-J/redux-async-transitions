# ovverride test code
test:
    override:
        - npm run test
        - npm run test-coverage

dependencies:
    pre:
        - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login

deployment:
    branch:
        master:
            - npm run upload-coverage #temporary
            - npm run tag
            - git push --tags
            - npm publish
